# Native deps whose install build scripts we DO allow (pnpm blocks build
# scripts by default). Listing them here builds them — avoiding the
# ERR_PNPM_IGNORED_BUILDS failure that fresh installs (CI / Vercel) hit.
onlyBuiltDependencies:
  - esbuild
  - sharp
  - unrs-resolver
verifyDepsBeforeRun: false
